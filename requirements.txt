# Instagram Username Extractor - Python Dependencies
# Tested on Python 3.9+, recommended: Python 3.11+
# Install with: pip3 install -r requirements.txt

# ========================================
# CORE OCR & IMAGE PROCESSING
# ========================================

# Primary OCR engine
easyocr>=1.7.0

# Image preprocessing and manipulation
opencv-python>=4.8.0,<5.0.0
Pillow>=10.0.0

# Array operations for image processing
numpy>=1.24.0

# EasyOCR backend and hardware acceleration
# PyTorch will auto-detect CUDA version and install appropriate binaries
torch>=2.0.0
torchvision>=0.15.0

# VLM integration (optional - graceful degradation if Ollama not installed)
# Can run with --no-vlm flag without this
ollama>=0.1.0

# Progress bars
tqdm>=4.66.0


# ========================================
# NOTION INTEGRATION & INSTAGRAM VALIDATION
# ========================================

# Official Notion Python SDK for database operations
notion-client>=2.2.1

# HTTP requests for Instagram profile validation
requests>=2.31.0

# Environment variable management (.env file)
python-dotenv>=1.0.0

# Retry logic with exponential backoff for robust API calls
tenacity>=8.2.3

# HTTP library (requests dependency, explicit for compatibility)
urllib3>=2.0.0


# ========================================
# CLI & INTERACTIVE PROMPTS
# ========================================

# Modern CLI framework with prompts
click>=8.1.0
