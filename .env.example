# ============================================================================
# Instagram Username Extractor - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual credentials
# cp .env.example .env
#
# Note: Most settings can be configured via the interactive CLI wizard
#       (extract-usernames --reconfigure)
# ============================================================================

# ----------------------------------------------------------------------------
# Notion Integration (Optional)
# ----------------------------------------------------------------------------
# Required only if you want to sync extracted usernames to Notion

# Your Notion integration token (starts with "secret_")
# Get it from: https://www.notion.so/my-integrations
# 1. Create new integration
# 2. Name it (e.g., "Instagram Leads")
# 3. Copy the Internal Integration Token
NOTION_TOKEN=secret_your_integration_token_here

# Your Notion database ID (32-character hex string)
# Get it from your database URL:
# https://notion.so/workspace/300472d4ce5181a4af1fc68082b64113?v=...
#                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#                           This is your database ID
NOTION_DATABASE_ID=your_database_id_here

# Skip Instagram profile validation when syncing to Notion
# Set to "true" to skip validation (faster but may include invalid profiles)
# Set to "false" to validate each profile (slower but more accurate)
# NOTION_SKIP_VALIDATION=false

# Delay between Instagram validation requests (in seconds)
# Increase if hitting rate limits
# NOTION_VALIDATION_DELAY=2

# Auto-sync to Notion after extraction (true/false)
# If false, you'll be prompted after each extraction
# NOTION_AUTO_SYNC=false

# ----------------------------------------------------------------------------
# Extraction Settings (Optional)
# ----------------------------------------------------------------------------
# These can override CLI configuration

# Default input directory for screenshots
# INPUT_DIR=~/Desktop/screenshots

# Default output directory for results
# OUTPUT_DIR=~/Desktop/leads

# Enable VLM mode (true/false)
# VLM_ENABLED=true

# VLM model to use (Ollama model name)
# VLM_MODEL=glm-ocr:bf16

# Enable diagnostics mode (true/false)
# Creates debug/ folder with detailed logs
# DIAGNOSTICS=false

# Number of parallel workers (leave empty for auto-detect)
# WORKERS=

# ----------------------------------------------------------------------------
# Advanced Settings (Optional)
# ----------------------------------------------------------------------------

# Ollama API endpoint (if running on different host)
# OLLAMA_HOST=http://localhost:11434

# Custom config file location
# CONFIG_PATH=~/.config/extract-usernames/config.json

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
